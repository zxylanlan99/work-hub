server {
    listen 80;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback so client-side routes (BrowserRouter) work on refresh.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
