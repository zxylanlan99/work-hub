# StudyMind（学思）— 问题日志

> 文档编号：SM-LOG-v1  
> 版本：v1.0  
> 日期：2026-06-19  
> 说明：记录开发过程中发现的问题、修正状态及遗留项。

---

## 一、数据模型对齐问题（db.js vs 物理数据模型设计 v1.0）

> 检查日期：2026-06-19  
> 检查文件：`src/js/db.js`  
> 依据文档：`docs/design/11-物理数据模型设计-v1.0.md`

### 1.1 已修正问题

| # | 集合 | 问题描述 | 修正内容 | 状态 |
|:-:|------|---------|---------|:----:|
| 1 | `goals` | `createGoal` 包含非设计字段 `domain`、`weeklyHours`、`currentLevel` | 移除三个多余字段 | ✅ 已修正 |
| 2 | `milestones` | `createMilestone` 包含非设计字段 `description` | 移除 `description` | ✅ 已修正 |
| 3 | `tasks` | 字段名不一致：代码用 `estimatedHours`，设计要求 `estimatedTime`（单位：分钟） | 改为 `estimatedTime` | ✅ 已修正 |
| 4 | `tasks` | `priority` 默认值不一致：代码 `'medium'`，设计 `'mid'` | 改为 `'mid'` | ✅ 已修正 |
| 5 | `tasks` | `createTask` 包含非设计字段 `aiType` | 移除 `aiType` | ✅ 已修正 |
| 6 | `knowledge_items` | 字段名不一致：代码用 `source`，设计要求 `sourceUrl` | 改为 `sourceUrl` | ✅ 已修正 |
| 7 | `knowledge_items` | 缺失字段：`level`、`deletedAt`、`readAt`、`readProgress` | 补全四个字段 | ✅ 已修正 |
| 8 | `knowledge_items` | 包含非设计字段 `status`、`relatedIds` | 移除多余字段 | ✅ 已修正 |
| 9 | `categories` | 字段名不一致：代码用 `icon`，设计要求 `color` | 改为 `color` | ✅ 已修正 |
| 10 | `review_cards` | 缺失字段：`questionType`、`lastQuality`、`reviewCount`、`lastReviewAt`、`updatedAt` | 补全五个字段 | ✅ 已修正 |
| 11 | `review_cards` | `mastery` 默认值不一致：代码 `0.3`，设计 `1.0` | 改为 `1.0` | ✅ 已修正 |
| 12 | `review_cards` | 包含非设计字段 `category`、`easeFactor` | 移除多余字段 | ✅ 已修正 |
| 13 | `news_items` | 字段名不一致：代码用 `source`，设计要求 `sourceName` | 改为 `sourceName` | ✅ 已修正 |
| 14 | `news_items` | 缺失字段：`category`、`tags`、`importance`、`knowledgeId`、`publishedAt` | 补全五个字段 | ✅ 已修正 |
| 15 | `news_items` | 包含非设计字段 `content`、`ignored` | 移除多余字段 | ✅ 已修正 |
| 16 | `chats` | 缺失字段 `knowledgeId`，包含非设计字段 `topic` | 补 `knowledgeId`，移除 `topic` | ✅ 已修正 |
| 17 | `messages` | 缺失字段：`model`、`tokens`、`cost`、`isStarred` | 补全四个字段 | ✅ 已修正 |
| 18 | `output_docs` | 缺失字段 `knowledgeIds` | 补全 `knowledgeIds` | ✅ 已修正 |
| 19 | `scraps` | 缺失字段：`source`、`outputDocId` | 补全两个字段 | ✅ 已修正 |
| 20 | `user_settings` | 创建时未写入默认值 | 补全 `defaultModel`、`dailyReviewCount`、`notificationEnabled`、`autoBackup` 默认值 | ✅ 已修正 |
| 21 | 全局 | `lastReviewed` 引用未对齐设计字段名 `lastReviewAt`（2 处） | 改为 `lastReviewAt` | ✅ 已修正 |
| 22 | `news_items` | `importNewsToKnowledge` 缺少 `level`、`tags`，未回写 `knowledgeId` | 补全字段并回写关联 | ✅ 已修正 |
| 23 | `news_items` | `getNewsOverviewStats` 使用 `imported` 字段，设计要求 `isSaved` | 改为 `isSaved` | ✅ 已修正 |
| 24 | `news_items` | `ignoreNews`/`batchIgnoreNews` 写入 `ignored` 字段（非设计） | 移除 `ignored` | ✅ 已修正 |
| 25 | `knowledge_items` | `mergeKnowledgeItems` 写入 `status: 'active'`（非设计） | 改为补全 `level`、`tags` | ✅ 已修正 |
| 26 | `knowledge_items` | `messageToKnowledge` 缺少 `level`、`tags` | 补全两个字段 | ✅ 已修正 |
| 27 | `knowledge_items` | `softDeleteKnowledgeItem` 缺少 `deletedAt` | 补写 `deletedAt: new Date()` | ✅ 已修正 |
| 28 | `review_cards` | `startReviewCard` 写入 `status: 'reviewing'`（非设计字段） | 改为写入 `lastReviewAt` | ✅ 已修正 |
| 29 | `knowledge_items` | `archiveKnowledge` 写入 `status: 'archived'`（非设计字段） | 改为 `level: 'secret'` | ✅ 已修正 |
| 30 | `knowledge_items` | `delayKnowledge` 写入 `status: 'delayed'`、`delayDays`（非设计字段） | 移除非设计字段 | ✅ 已修正 |
| 31 | `knowledge_items` | `linkKnowledge` 写入 `relatedIds`（非设计字段） | 改为使用 `tags` 字段 | ✅ 已修正 |
| 32 | `knowledge_items` | `batchImportItems` 写入 `status: 'active'`（非设计字段） | 改为 `isDeleted: false` | ✅ 已修正 |
| 33 | `knowledge_items` | `batchIgnoreItems` 写入 `status: 'ignored'`（非设计字段） | 改为 `level: 'secret'` | ✅ 已修正 |

---

### 1.2 遗留问题（待修正）

| # | 集合 | 函数 | 行号 | 问题描述 | 建议修正 |
|:-:|------|------|:----:|---------|---------|
| L1 | `messages` | `adoptAnswer` | 1013 | 写入 `adopted: true`，设计中 `messages` 无此字段 | 移除 `adopted`，或改用 `isStarred` 表示采纳 |
| L2 | `news_items` | `batchDelayNews` | 1181 | 写入 `delayedUntil`，设计中 `news_items` 无此字段 | 移除 `delayedUntil`，或在设计文档中补充该字段 |
| L3 | `scraps` | `ignoreScrap` | 1299 | `status: 'ignored'` 不在设计枚举值内（设计仅 raw/processing/converted） | 改为 `status: 'raw'` 或在设计中补充 `ignored` 枚举值 |
| L4 | `output_docs` | `expandScrapToDoc` | 1288 | 创建 `output_docs` 时缺少 `knowledgeIds` 字段 | 补写 `knowledgeIds: []` |
| L5 | `messages` | `mergeCompareAnswers` | 1020 | 创建 `messages` 时缺少 `model`、`tokens`、`cost`、`isStarred` | 补全四个字段 |

---

## 二、问题统计

| 类别 | 数量 |
|------|:----:|
| 已修正问题 | 33 |
| 遗留问题 | 5 |
| **合计** | **38** |

---

*文档结束。后续发现的新问题请追加至对应分类表格中。*
