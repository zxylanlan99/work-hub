# StudyMind（学思）— 测试验收方案 v2.0

> 文档编号：SM-TEST-v2  
> 版本：v2.0  
> 日期：2026-06-12  
> 依据：`05-PRD-产品需求文档-v3.0.md`、`06原型设计方案汇总.md`、`07prototypes/*.html`、`09-产品详细设计-v2.md`、`10-物理数据模型设计-v1.0.md`、`12-接口设计-v1.0.md`  
> 变更说明：v2.0 完全重写接口测试章节，从 RESTful 33 接口改为 CloudBase SDK + 云函数测试；更新所有文档引用为新编号。

---

## 一、测试目标

| 目标 | 说明 |
|------|------|
| **UI 一致性** | 前端页面布局、组件样式、交互行为与原型保持一致 |
| **功能完整性** | 所有 PRD v3.0 §八定义的功能点（CRUD + AI 节点 + 业务规则）均已实现 |
| **数据层正确性** | CloudBase SDK 数据操作（13 集合 CRUD）与物理模型设计一致 |
| **云函数正确性** | ai-proxy 流式调用、模型路由、费用计算正确；data-cleanup 定时清理正确 |
| **安全规则** | 用户数据严格隔离（_openid），跨用户访问被拒绝 |
| **认证流程** | 匿名登录 → 数据归属 → 持久化 全流程正确 |

---

## 二、全局验收点

### 2.1 主题与样式（所有页面）

| ID | 验收点 | 依据 |
|----|--------|------|
| G-001 | 统一白色/浅色主题（`background: #f0f2f5`），无暗色主题切换功能 | `06原型设计方案汇总.md` §1.1 |
| G-002 | CSS 变量体系：`--primary: #6366f1`、`--success: #22c55e`、`--warning: #f59e0b`、`--danger: #ef4444`、`--info: #3b82f6` | `06原型设计方案汇总.md` §1.1 |
| G-003 | 灰色体系完整：`--gray-50` 到 `--gray-900` | `06原型设计方案汇总.md` §1.1 |

### 2.2 全局布局（所有页面）

| ID | 验收点 | 依据 |
|----|--------|------|
| G-004 | 左侧边栏宽度 220px，背景 `#f9fafb`，右侧 1px `--gray-200` 分割线 | `06原型设计方案汇总.md` §1.2 |
| G-005 | Logo 区域显示 "🧠 StudyMind"，字体 18px/bold/`--primary` 色 | 所有原型 |
| G-006 | 顶栏高度 56px，白色背景 + 底部分割线 `--gray-200` | `06原型设计方案汇总.md` §1.2 |

### 2.3 左侧菜单（所有页面）

| ID | 验收点 | 依据 |
|----|--------|------|
| G-007 | 菜单项顺序：📊 首页 → 🎯 学习计划 → 📡 资讯 → 📚 知识库 → 💬 AI对话 → 🔁 复习计划 → ✍️ 知识沉淀 | `06原型设计方案汇总.md` §1.3 |
| G-008 | 当前页面菜单项高亮：`background: var(--primary-light)` + `color: var(--primary)` + `font-weight: 600` | 所有原型 |
| G-009 | 非激活项 hover：`background: var(--gray-200)` | 所有原型 |

### 2.4 通用组件（所有页面）

| ID | 验收点 | 依据 |
|----|--------|------|
| G-010 | `.btn-primary`：`background: var(--primary)` / hover `var(--primary-dark)` / 白色文字 | 所有原型 |
| G-011 | 弹窗：遮罩层 `rgba(0,0,0,.4)` + 居中白色卡片 + `fadeIn` 动画 | 所有原型 |
| G-012 | Toast：右上角 + 滑入 + 3s 消失 + 左边框颜色区分 | 所有原型 |

> **注**：§二 全局验收点与原型完全一致，无需修改。以下 §三~§十 为各模块功能验收点。

---

## 三、首页模块验收点

### 3.1 页面结构

| ID | 验收点 |
|----|--------|
| H-001 | 暖身卡片：昨日回顾 1 句 + 快问快答 1 题 + [继续]/[跳过] 按钮 |
| H-002 | 四宫格快捷入口：学习计划/今日复习/待处理资讯/输出草稿，点击跳转对应模块 |
| H-003 | 智能续接：上次学习位置 + 预计剩余时间 + [继续] 按钮 |
| H-004 | 学习日历热力图：近 2 周，颜色深浅 4 级 |
| H-005 | 本周趋势：复习完成率 + 环比 + 连续天数 |

### 3.2 暖身卡片（PRD 1C 节点）

| ID | 验收点 |
|----|--------|
| H-006 | 快问快答点击 [查看答案] → 展开绿色答案标签 |
| H-007 | [跳过暖身] → 整条暖身卡隐藏 |

---

## 四、资讯模块验收点

### 4.1 AI 推荐清单

| ID | 验收点 |
|----|--------|
| N-001 | 5 个 Tab：全部/强烈推荐/推荐/可选/已处理，切换正常 |
| N-002 | 五维评分展开显示：可信度/价值/知识对齐/转化难度/时效 |
| N-003 | 批量操作：勾选多条 → [批量入库] → 确认弹窗 |

### 4.2 入库闭环（PRD 3A~3F 节点）

| ID | 验收点 |
|----|--------|
| N-004 | 手动录入 URL → AI 自动抓取原文 → 五维评分 → 预览 |
| N-005 | [确认入库] → 弹窗：标题/分类/标签/摘要可编辑 → 确认 → 写入 `knowledge_items` |
| N-006 | 入库成功 → `knowledge_items` 记录 +1，`news_items.isSaved = true` |
| N-007 | 同时生成复习卡片 → `review_cards` 新增记录 |

---

## 五、学习计划模块验收点

### 5.1 目标详情

| ID | 验收点 |
|----|--------|
| P-001 | 进度大环显示正确百分比（tasks completed / tasks total）|
| P-002 | 里程碑展开/折叠正常，任务勾选状态流转（pending → completed）|
| P-003 | hover 任务卡片显示 [编辑]/[删除] 操作按钮 |

### 5.2 新建目标（PRD 2A~2C 节点）

| ID | 验收点 |
|----|--------|
| P-004 | 点击 [+ 新建目标] → 弹窗 step1：填写描述 |
| P-005 | 点击 [下一步] → step2：AI 澄清对话（多轮）→ 生成方案 |
| P-006 | 确认方案 → 写入 `goals` + `milestones` + `tasks` |

### 5.3 弹性排期（PRD 2D 节点）

| ID | 验收点 |
|----|--------|
| P-007 | 任务逾期 → 弹窗提供 3 方案：顺延/跳过/拆分 |
| P-008 | 选择顺延 → 后续任务 `deadline` 批量 +N 天 |

---

## 六、知识库模块验收点

### 6.1 分类与条目

| ID | 验收点 |
|----|--------|
| K-001 | 左：三级分类树展开/折叠/搜索正常 |
| K-002 | 中：子 Tab 切换（全部条目/AI推荐/回收站）|
| K-003 | 知识卡片显示标题/分类标签/阅读进度条 |

### 6.2 过期提醒（PRD 5B 节点）

| ID | 验收点 |
|----|--------|
| K-004 | 技术类条目 >1 年未读 → 顶部显示橙色提醒栏 |
| K-005 | 非技术类 >3 年 → 提醒栏显示 |

### 6.3 软删除与回收站

| ID | 验收点 |
|----|--------|
| K-006 | 删除知识条目 → `isDeleted = true`，主列表不显示 |
| K-007 | 回收站：显示已删除条目，[恢复] → `isDeleted = false` |
| K-008 | [永久删除] → 二次确认 → `collection('knowledge_items').doc(id).remove()` |

---

## 七、AI 对话模块验收点

### 7.1 对话管理

| ID | 验收点 |
|----|--------|
| C-001 | 左侧对话列表按时间分组（今天/昨天/本周）|
| C-002 | 新建对话 → `chats` 新增记录，自动生成标题 |
| C-003 | 删除对话 → 应用层级联删除 `messages` |

### 7.2 模型路由（PRD 4A 节点）

| ID | 验收点 |
|----|--------|
| C-004 | 模型下拉 5 选，切换后新消息使用该模型 |
| C-005 | 自动路由标识：≥80% 绿色 / 50-79% 蓝色 / <50% 灰色 |
| C-006 | 费用预估显示（调用前），实际扣费与 `messages.cost` 一致 |

### 7.3 收藏入库

| ID | 验收点 |
|----|--------|
| C-007 | AI 回复下方 [⭐ 收藏] → 弹出入库确认弹窗 |
| C-008 | 确认 → 写入 `knowledge_items`，`messages.isStarred = true` |

---

## 八、复习计划模块验收点

### 8.1 今日复习（PRD 6A 节点）

| ID | 验收点 |
|----|--------|
| R-001 | 加载今日待复习卡片（`nextReview <= today`），按掌握度升序 |
| R-002 | 卡片翻转：正面选择题 → 点击 [翻转] → 背面答案 + SM-2 自评 4 档 |
| R-003 | 自评后即时显示：间隔天数/下次复习日/掌握度变化 |

### 8.2 遗忘预警（PRD 6D 节点）

| ID | 验收点 |
|----|--------|
| R-004 | 掌握度 < 2.0 且距离上次复习 > 间隔 × 2 → 红色预警标签 |
| R-005 | 点击 [薄弱点补强] → 跳转知识库对应条目 |

---

## 九、知识沉淀模块验收点

### 9.1 分屏编辑器

| ID | 验收点 |
|----|--------|
| O-001 | 左 55% Markdown 编辑，右实时预览，同步滚动 |
| O-002 | 右侧 AI 辅助面板：素材推荐 top3 + [引用] 按钮 |
| O-003 | [引用] → Markdown 插入 `> 来源：《XXX》` |

### 9.2 AI 辅助（PRD 7A~7F 节点）

| ID | 验收点 |
|----|--------|
| O-004 | [AI 生成初稿] → 调用 `ai-proxy` → 流式返回 → 插入编辑器 |
| O-005 | [AI 质量评审] → 四维评分 + 改进建议 → 右上角悬浮提示 |

---

## 十、系统设置模块验收点

### 10.1 设置分区

| ID | 验收点 |
|----|--------|
| S-001 | 无主题切换功能（产品统一白色），设置页不含主题选项 |
| S-002 | AI 配置：默认模型下拉 + 月度预算显示（¥0.00 / ¥5.00）|
| S-003 | 通知开关：复习提醒 Toggle 切换即时生效 |
| S-004 | 数据管理：[导出 JSON] → 下载文件；[导入 JSON] → 预览 → 确认覆盖 |

---

## 十一、CloudBase 数据层测试（替代原 RESTful 接口测试）

> ⚠️ **架构变更说明**：新架构无 RESTful 接口，所有数据库操作通过 CloudBase JS SDK 直连。本节测试 13 集合的 CRUD 操作与物理模型设计的一致性。

### 11.1 测试环境准备

| 准备项 | 说明 |
|--------|------|
| CloudBase 环境 | 测试环境（env-id: `studymind-test`） |
| 测试数据 | 每个集合预置 5-10 条测试数据 |
| 安全规则 | 设置为增强型（`read/write: auth != null && auth.uid == doc._openid`）|
| 浏览器 | Chrome / Firefox / Safari / Edge 最新两版 |

### 11.2 集合 CRUD 测试矩阵

| 集合 | 创建 | 查询 | 更新 | 删除/软删除 | 索引验证 |
|------|:----:|:----:|:----:|:----------:|---------|
| `goals` | ✅ | ✅ `status` 筛选 | ✅ | ✅ 级联 | ✅ `_openid+status` |
| `milestones` | ✅ | ✅ `goalId` | ✅ | ✅ 级联 | ✅ `goalId+sort` |
| `tasks` | ✅ | ✅ `milestoneId` | ✅ 状态流转 | ✅ | ✅ `milestoneId+sort` |
| `knowledge_items` | ✅ | ✅ `isDeleted:false` | ✅ | ✅ 软删除 | ✅ `_openid+isDeleted` |
| `categories` | ✅ | ✅ 全量+树构建 | ✅ | ✅ 安全删除 | — |
| `review_cards` | ✅ | ✅ `nextReview<=today` | ✅ SM-2 | ✅ 标记失效 | ✅ `_openid+nextReview` |
| `review_history` | ✅ | ✅ `cardId` | — | — | ✅ `cardId+reviewedAt` |
| `news_items` | ✅ | ✅ `hasRead` Tab | ✅ 标记 | — | ✅ `_openid+hasRead` |
| `chats` | ✅ | ✅ 时间倒序 | ✅ 标题 | ✅ 级联 | — |
| `messages` | ✅ | ✅ `chatId+createdAt` | ✅ 收藏 | — | ✅ `chatId+createdAt` |
| `output_docs` | ✅ | ✅ `status` | ✅ 自动保存 | ✅ | ✅ `_openid+status` |
| `scraps` | ✅ | ✅ 最新 | ✅ 转化 | — | — |
| `user_settings` | ✅ 自动 | ✅ 读取 | ✅ 更新 | — | — |

### 11.3 关键查询测试（手写测试用例示例）

```javascript
// 测试用例 TC-DB-01：今日待复习队列加载
async function testTodayReviewQueue() {
  // 1. 预置数据：创建 3 张复习卡片，nextReview 分别为 昨天/今天/明天
  await db.collection('review_cards').add([
    { question: 'Q1', answer: 'A1', nextReview: yesterday, mastery: 3, knowledgeId: 'xxx', _openid: uid },
    { question: 'Q2', answer: 'A2', nextReview: today, mastery: 2, knowledgeId: 'xxx', _openid: uid },
    { question: 'Q3', answer: 'A3', nextReview: tomorrow, mastery: 4, knowledgeId: 'xxx', _openid: uid },
  ])
  
  // 2. 执行查询
  const { data } = await db.collection('review_cards')
    .where({ _openid: uid, nextReview: db.command.lte(today) })
    .orderBy('mastery', 'asc')
    .get()
  
  // 3. 断言：只返回 Q1 和 Q2，Q3 不返回
  console.assert(data.length === 2, '应返回 2 张卡片')
  console.assert(data[0].question === 'Q2', '掌握度低的排前面')
}

// 测试用例 TC-DB-02：软删除过滤
async function testSoftDelete() {
  await db.collection('knowledge_items').add({
    title: '测试条目', content: '...', isDeleted: false, _openid: uid
  })
  
  // 软删除
  await db.collection('knowledge_items').doc(id).update({ isDeleted: true, deletedAt: new Date() })
  
  // 主列表查询应排除
  const { data } = await db.collection('knowledge_items')
    .where({ _openid: uid, isDeleted: false })
    .get()
  
  console.assert(data.length === 0, '软删除条目不应出现在主列表')
}
```

---

## 十二、云函数测试

### 12.1 `ai-proxy` 测试

| ID | 测试场景 | 入参 | 预期出参 |
|----|---------|------|---------|
| CF-001 | 正常调用 MiMo | `{ messages: [{role:'user',content:'你好'}], model:'mimo' }` | `{ content, tokens, model, cost }` |
| CF-002 | 不传 model（自动路由）| `{ messages: [...] }` | 使用 MiMo（默认）|
| CF-003 | 模型路由切换 | 检测到代码块 → 自动切换 DeepSeek | `model: 'deepseek-v3'` |
| CF-004 | 频率限制 | 短时间内连续 10 次调用 | 第 6 次起返回 `RATE_LIMITED` |
| CF-005 | 超时处理 | AI API 15s 未响应 | 返回 `TIMEOUT`，前端提示 |
| CF-006 | 费用计算 | 调用后检查 `cost` 字段 | `cost = tokens / 1000 * unitPrice` |

### 12.2 `data-cleanup` 测试

| ID | 测试场景 | 触发方式 | 预期结果 |
|----|---------|---------|---------|
| CF-101 | 清理软删除超过 30 天的知识 | 定时触发（凌晨 2:00）或手动 | 对应记录被永久删除 |
| CF-102 | 归档 90 天前已转化资讯 | 定时触发 | `news_items.archived = true` |
| CF-103 | 清理 1 年前复习记录 | 每周触发 | 旧记录被删除 |

---

## 十三、Auth 认证流程测试

| ID | 测试场景 | 操作步骤 | 预期结果 |
|----|---------|---------|---------|
| AUTH-001 | 首次打开 → 匿名登录 | 打开 `index.html` | `app.auth().getLoginState()` 返回 uid |
| AUTH-002 | 登录态持久化 | 刷新页面 | 无需重新登录 |
| AUTH-003 | 数据归属 | 匿名登录后创建知识条目 | `knowledge_items._openid === uid` |
| AUTH-004 | 跨用户隔离 | 用另一个浏览器/隐私模式打开 | 看不到前一个用户的数据 |
| AUTH-005 | 登出后 | `app.auth().signOut()` | 重新匿名登录，uid 可能变化（数据留在旧 uid）|

> ⚠️ **注意**：匿名登录的 uid 在登出后无法恢复。正式推广时建议引导用户绑定微信登录（V2）。

---

## 十四、安全规则验证测试

> 在 CloudBase 控制台将安全规则临时修改为"开放"（读/写无限制），执行以下测试，然后恢复为增强型规则，确认跨用户访问被拒绝。

| ID | 测试场景 | 安全规则 | 预期 |
|----|---------|---------|------|
| SEC-001 | 用户 A 读取用户 B 的数据 | 开放 → 恢复增强型 | 恢复后返回空数组（非错误）|
| SEC-002 | 用户 A 修改用户 B 的条目 | 增强型（`_openid == auth.uid`）| 更新失败，前端 catch 报错 |
| SEC-003 | 未登录用户读取数据 | 未登录 | 所有请求被拒绝（`auth == null`）|

**安全规则 JSON（增强型）**：

```json
{
  "read": "auth != null && auth.uid == doc._openid",
  "write": "auth != null && auth.uid == doc._openid"
}
```

---

## 十五、测试执行清单

### 15.1 执行顺序

```
阶段 1：Auth 认证（§十三）+ 安全规则（§十四）
  ↓
阶段 2：数据层 CRUD（§十一）→ 每个集合逐条验证
  ↓
阶段 3：云函数（§十二）→ ai-proxy + data-cleanup
  ↓
阶段 4：UI 一致性（§二）+ 各模块功能（§三~§十）→ 浏览器手动验证
```

### 15.2 验收通过标准

| 维度 | 通过标准 |
|------|---------|
| 数据层 | §十一 13 集合 CRUD 全部通过，索引查询性能 < 500ms |
| 云函数 | §十二 CF-001~CF-006 全部通过，ai-proxy 响应 < 15s |
| 认证 | §十三 AUTH-001~AUTH-005 全部通过 |
| 安全 | §十四 SEC-001~SEC-003 全部通过（跨用户访问被拒绝）|
| UI | §二 G-001~G-012 + §三~§十 全部验收点通过 |
| 原型 | 8 个原型 HTML 与产品代码渲染结果一致 |

---

*文档结束。与 `05-PRD-产品需求文档-v3.0.md`（功能需求）、`10-物理数据模型设计-v1.0.md`（数据规范）、`12-接口设计-v1.0.md`（接口规范）配套使用。*
